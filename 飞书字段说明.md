# 飞书表格字段配置说明

## 当前表格字段
根据 API 返回，当前表格有以下字段：
- SKU
- 仓库
- 入库单号
- 数量
- 日期
- 是否送到
- 跟进人
- 运输方式
- 预计到达时间（空运 10 天，海运 35 天）

## 需要添加的字段
要使用物流查询功能，需要在飞书多维表格中添加以下字段：

1. **最新状态** (文本类型) - 存放最新物流状态
2. **最新时间** (文本类型或日期时间) - 存放最新状态时间
3. **物流摘要** (文本类型) - 存放三节点摘要（如：1.13 下单 -1.17 义乌入库 -2.7 送达）

## 添加字段步骤
1. 打开飞书多维表格
2. 点击表头右侧的「+」添加字段
3. 选择字段类型（文本）
4. 输入字段名（必须与上面完全一致）
5. 重复添加其他字段

## 或者修改脚本使用现有字段
如果不想添加新字段，可以修改 `feishu_query_v2.py` 中的字段映射，使用现有字段存储结果。
